[project]
name = "killer-sudoku"
version = "0.1.0"
description = "A Tkinter Killer Sudoku game with a neural-guided solver"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "torch>=2.11.0",
]
